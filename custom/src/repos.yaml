# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    odoo: https://github.com/odoo/odoo.git
    openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: ocb $ODOO_VERSION
  merges:
    - ocb $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head
l10n-france:
  defaults:
    depth: 200
  remotes:
    origin: https://github.com/OCA/l10n-france.git
    zolfariot: https://github.com/zolfariot/l10n-france.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/682/head # SIRENE lookup
    - origin refs/pull/707/head # New FEC export
    - origin refs/pull/628/head # l10n_fr_account_oca: VAT return dependency
    #- zolfariot refs/pull/1/head # New French VAT return
    - zolfariot refs/pull/2/head # French VAT reporting
account-reconcile:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/account-reconcile.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/1015/head # account_reconcile_oca: partial vendor bill payment suggests full residual
bank-payment:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/bank-payment.git
    zolfariot: https://github.com/zolfariot/bank-payment.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - zolfariot refs/pull/1/head # Qonto API Payments
bank-statement-import:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/bank-statement-import.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/875/head # Qonto
purchase-workflow:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/purchase-workflow.git
    zolfariot: https://github.com/zolfariot/purchase-workflow.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - zolfariot refs/pull/1/head # purchase_fixed_discount
edi:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/edi.git
    zolfariot: https://github.com/zolfariot/edi.git
  merges:
    - origin $ODOO_VERSION
    - zolfariot refs/pull/4/head # hosted invoice import
community-data-files:
  defaults:
    depth: 100
  remotes:
    origin: https://github.com/OCA/community-data-files.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/277/head # for e-invoicing: account_tax_unece: add VAT exemption reason codes (VATEX)
fr-einvoicing:
  defaults:
    depth: 10
  remotes:
    origin: https://github.com/akretion/fr-einvoicing.git
    zolfariot: https://github.com/zolfariot/fr-einvoicing.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - zolfariot refs/pull/1/head # Res company broken dep - #1
l10n-france-zolfa:
  remotes:
    origin: git+ssh://git@github.com/zolfariot/l10n-france-zolfa.git
  merges:
    - origin main
    - origin refs/pull/2/head # x-factur product map helper
  target: origin main


